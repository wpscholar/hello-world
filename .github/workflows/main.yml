name: On Push

on: [push]
#  release:
#    types:
#      - created

env:
  REPO_NAME: hello-world

jobs:
  build:
    name: Run

    runs-on: ubuntu-latest

    steps:

    - name: Checkout
      uses: actions/checkout@v2

    - name: PHP version
      run: php --version

    - name: Get Payload Information
      id: payload_info
      uses: ./.github/actions/get-event-payload
